# 更新日志

本文件记录每个公开版本的变化。开发过程中的完整查证与踩坑记录见源码包内的
`README-镜维度.md` 与开发日志，这里只写"玩家能感知到的变化"。

下载：本仓库根目录的 `mirrorside-<版本>.jar`（模组本体）+ `mirrorside-<版本>-源码.zip`（配套脚本与完整源码）。
旧版本文件保留，便于对照回退。

---

## 0.3

> 主题：**波粒子**与**波粒观测**两套新玩法，加上容器保真、材质重做与配方大扩容。

### 新增：波粒子（把任意方块变成"会掉落的重力方块"）

- 新增物品链：**波 → 粒 → 波粒子**（工作台可互相转化），用波粒子右键方块就能给它加上重力特性 ——
  它会在下一次邻接更新时坠落，并在分界线处按重力反转往复。
- 每格方块**只生效一次**：改造 → 坠落 → 反转 → 落定，不会无限循环。
- **带方块实体的方块也能推**（刷怪笼、试炼刷怪笼、箱子、木桶、熔炉……），
  挖不动的方块（基岩、强化深板岩一类）同样可以推下去。
- 参数全部在 `kubejs/startup_scripts/mirror_config.js` 的 `waveParticle` 段：启用开关、
  是否消耗物品、下坠延迟、是否允许方块实体、每维度上限、是否提示。

### 新增：波粒观测（多方块 + 望远镜加工）

- 用**四个波粒块**围住中间那格（四周配荧石与紫水晶母岩），中间放要观测的方块，
  拿**望远镜**右键它 —— 等价于"把这套引力配方手动跑一遍"，不必真的扔下去：
  方块→方块、方块→物品、方块→实体。
- 结构要求**整座完全一致**（21 格），四个朝向都认；结构里有一格不对称的晶芽，
  所以要旋转就得整座一起转。除这 21 格外其它位置放什么都行。
- **复用全部引力配方**：穿越类直接出结果（红沙 → 金块），接触类看靶心正下方
  （红沙 + 下面放荧石 → 荧石）。
- 专用配方：冰 → 蓝冰；羊毛（下方放锁链）→ 蜘蛛网；灵魂沙 → 下界疣（掉落物）；
  蛋糕 → 悦灵（实体）。
- **染色方块随机换色**：羊毛、地毯、混凝土、陶瓦、玻璃、床、旗帜、蜡烛等
  13 个族（每族 16 色）在观测台上可以随机变成**同族的另一种颜色**。
- 加工**不发聊天栏消息**（只给粒子与音效），排查看服务端日志或 `/mirror diag`。
- **JEI 可查**：新增「波粒观测」分类，左侧工作方块就是**波粒块**，
  能在 JEI 里搜索与查看（含"下方要放什么"那一格）。

### 配方：46 条 → **535 条**

- 木材 / 铜 / 石材三大**家族配方**全面展开（台阶、楼梯、墙、砖、抛光、氧化铜……
  同级之间互相转化，基准方块回退）。
- 新增：波粒链、干海带块 → 海晶石 → 海晶灯、骨块 → 山羊角（8 种乐器随机）、
  荧石三条、幽匿系列（催化剂 / 传感器 / 尖啸体）、黑曜石 → 哭泣的黑曜石、
  金块 → 钟、深板岩 → 强化深板岩等。
- 混凝土粉末 32 条：×8 → 陶瓦，落在熔炉上 ×4 → 釉面陶瓦。
- JEI 数据同步为 535 条，另有 552 条波粒观测配方。

### 修复

- **容器不会再"刷物品"**：改造箱子/木桶时内容物不再掉一地（原版 `onRemove` 会触发掉落，
  而现在同一份数据还会随方块落地写回，等于凭空多一份）。
- **方块实体数据随掉落物保留**：箱子/木桶被推成掉落物后捡起来重新摆出，**内容物仍在**
  （连同自定义名字与锁）；刷怪笼的实体数据同理。
- **修掉一次存档崩溃**：上一版掉落物带的数据缺少 `id` 字段，会导致该物品无法序列化，
  玩家存档时直接崩（"Missing id for entity"）。现在写入前会校验，并且**进世界时会自愈**
  背包里已经存在的坏数据。
- **KubeJS 加载期不再刷假报错**：早前 8 条"山羊角 pool 无法解析"是校验在服务器注册表
  就绪前误判，配方本身没有问题。

### 其他

- **传送门材质重做**：48 帧动态贴图（约 4.8 秒一轮），黑白过渡改为连续渐变，不再有硬条纹。
- **镜面方块材质重做**：连续银蓝渐变 + 一道对角高光，看起来像镜面而不是瓷砖。
- **镜维度永远晴天**（原版天气循环对整个维度跳过），保留天空光。
- **针尖对麦芒**（spindle）结构：镜维度里会生成的地标建筑，参数可在配置里调。
- 离线校验从 13 项扩到 **14 项**，全部通过；`/mirror diag` 增加波粒子与波粒观测两节。

---

## 0.2

- 版号 0.1 → 0.2（模组元数据、产物文件名同步）。
- **删除**配方「脚手架 ×4 + 落在灵魂沙上 → 地狱疣」及其两套专用机制
  （"碰到过就算数"与"按竖列计数"）。脚手架现在只有 2 条配方：×8 → 蜘蛛网、×4 + 树叶 → 树苗。
- 修复：同一竖列的多个下落方块**共用计数器**导致切换次数暴涨、瞬间全部变蜘蛛网的问题
  （现在一律按实体计数）。
- 排查体验：`/mirror recipes` 新增「落点该摆在哪 x/z」与「它这一列实际有什么 / 你要的就在隔壁差几格」，
  专治"落点摆错一列 = 看起来像配方坏了"。
- JEI 数据同步：46 条配方（去掉地狱疣那条）。

---

## 0.1

- 首个公开版本：镜维度、重力反转、传送门、重力方块加工（47 条）、工作台配方。
